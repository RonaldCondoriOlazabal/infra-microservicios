server:
  port: 7071
spring:
  application:
    name: config-server
  profiles: 
    active: native 
  cloud:
    config:
      server:
        native: 
          #search-locations: file:///C:/ms1/ProyectosMS2026/infra/config-repo 
          #search-locations: file:/config-repo
          # En Windows/Linux: permite override por variable de entorno.
          # Por defecto, usa el config-repo del monorepo (relativo a este módulo).
          search-locations: ${CONFIG_REPO_LOCATION:file:../config-repo}

